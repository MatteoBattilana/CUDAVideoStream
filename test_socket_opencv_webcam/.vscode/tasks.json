{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build-all",
      "type": "shell",
      "options": {
          "cwd": "${workspaceFolder}",
          "env": {
          }
      },
      "command": "make -f Makefile m1",
    },
    {
      "label": "image_reader",
      "type": "shell",
      "command": "gcc -g ${file} -o ${fileBasenameNoExtension}"
    }
  ]
}